<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>BETA REVIEWS</title>
    <!-- Site Favicon -->
    <link rel="shortcut icon" type="image/png" href="images/Beta-Review.png" />
    <!-- Bootstrap Css CDN File -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
    <!-- Semantic Css CDN File -->
    <link rel="stylesheet" type="text/css" href="https://cdnjs.cloudflare.com/ajax/libs/semantic-ui/2.4.1/semantic.min.css" />
    <!-- Google fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Amatic+SC:wght@700&family=Anton&family=Architects+Daughter&family=Great+Vibes&family=Kaushan+Script&family=Sacramento&family=Bangers&display=swap" rel="stylesheet">
    <!--Multi-Step-Form css file-->
    <link rel="stylesheet" href="styles/multi-step-form.css">
    <!-- Owl Carousel Css -->
    <link rel="stylesheet" href="styles/owl.carousel.min.css">
    <link rel="stylesheet" href="styles/owl.theme.default.min.css">
    <!-- Custom Css File -->
    <link rel="stylesheet" type="text/css" href="/styles/style.css" />

</head>

<body>
    <div class="ui sidebar inverted vertical menu">
        <a href="/" class="item"> <i class="home icon"></i> Home</a>
        <a href="/reviews" class="item"> <i class="comments icon"></i> Reviews</a>
        <a href="/#add-review-form" class="item"> <i class="plus circle icon"></i> Add Review</a>
        <% if(!currentUser){ %>
            <a href="/signup" class="item"> <i class="user plus icon"></i> Sign Up</a>
            <a href="/login" class="item"><i class="user icon"></i> Login</a>
            <% }else { %>
                <a href="#" class="item">
                        Signed In As :
                        <span style="color:#32CD32;"><%= currentUser.username %></span>
                        <i class="user circle icon"></i>
                </a>
                <a href="/logout" class="item"> <i class="power off icon"></i> Logout</a>
                <% } %>
    </div>
    <div class="pusher">
        <div class="nav">
            <div class="nav-content">
                <div class="nav-brand">
                    <a href="/">BETA REVIEW</a>
                </div>
            </div>
        </div>
        <button id="menu-toggle" class=" ui icon button">
            <i class="chevron right icon"></i>
        </button>
        <div class="flash container">
            <% if(error && error.length > 0){%>
                <div class="alert alert-danger alert-dismissible fade show" role="alert">
                    <%= error %>
                        <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                                <span aria-hidden="true">&times;</span>
                            </button>
                </div>

                <%}%>
                    <%if(success && success.length > 0){%>
                        <div class="alert alert-success alert-dismissible fade show" role="alert">
                            <%= success %>
                                <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                                        <span aria-hidden="true">&times;</span>
                                    </button>
                        </div>
                        <%}%>
        </div>

</body>

</html>